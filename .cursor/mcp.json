{
  "mcpServers": {
    "ast-index": {
      "type": "stdio",
      "command": "node",
      "args": [
        "${workspaceFolder}/scripts/ast-index-mcp.js"
      ],
      "env": {
        "AST_INDEX_ROOT": "${workspaceFolder}",
        "AST_INDEX_DB_PATH": "${workspaceFolder}/.ast-index/index.db"
      }
    }
  }
}
